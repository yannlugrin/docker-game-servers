# Every tool this repository uses, pinned. `make setup` installs them into
# .venv; pre-commit runs them from there (language: system), so there is one
# source of truth for versions.
check-jsonschema==0.38.0
pre-commit==4.6.2
pymarkdownlnt==0.9.39
pytest==9.1.1
ruff==0.16.2
yamllint==1.38.0
