# Decisions — steamcmd track

Decision log for choices governing `steamcmd/` (CLAUDE.md rule 4).
Entry format: `D-NNN` id (file order, frozen once assigned, never
reused; ids are per-log — cross-log citations name the file, e.g.
`DECISIONS.md D-002` for the root log), date, plan step, context,
decision, alternatives considered, approved by. Entries citing
not-yet-started steps give number *plus title*.

No entries yet.
