# steamcmd Builder Image — Specification

The steamcmd builder image is specified in the repository-root
`SPECIFICATIONS.md`: §4 is its section, resting on the facts of §2 and the
core model of §3. This file adds no requirements of its own; it exists so
every image directory carries the same layout.
